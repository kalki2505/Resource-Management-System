export declare type ThumbnailResult = {
    uri: string;
    width: number;
    height: number;
};
declare type PdfThumbnailType = {
    generate(filePath: string, page: number): Promise<ThumbnailResult>;
    generateAllPages(filePath: string): Promise<ThumbnailResult[]>;
};
declare const _default: PdfThumbnailType;
export default _default;
